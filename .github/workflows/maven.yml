name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Schritt 1: Checkout
      - uses: actions/checkout@v4

      # Schritt 2: Setup JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      # Schritt 3: Build with Maven
      - name: Build with Maven
        working-directory: ./DevOpsCalc
        run: mvn -B package

      # Schritt 4: Junit Test
      - name: Junit Test Dashboard
        uses: janisz/action@v2
        with:
          paths: target/surefire-reports/*.xml
          output: test-results.md
          show: all

      # Schritt 5: SonarQube Scan
      - name: SonarQube Analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn sonar:sonar \
          -Dsonar.projectKey=DevOpsCalc \
          -Dsonar.host.url=https://your-sonarqube-server-url \
          -Dsonar.login=${{ secrets.SONAR_TOKEN }}
